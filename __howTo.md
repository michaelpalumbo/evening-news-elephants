
1. enter the virtual environment: ```shell python3 -m venv venv```
2. activate it ```shell source venv/bin/activate```
3. if first time running on machine, install libs ```shell pip install pdfplumber pyusb```